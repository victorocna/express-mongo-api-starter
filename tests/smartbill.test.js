import { assert } from 'chai';
import { it } from 'mocha';

export default function () {
  it('test SmartBill plugin', async function () {
    // Arrange
    // Act
    // Assert
    assert.isTrue(true);
  });
}
